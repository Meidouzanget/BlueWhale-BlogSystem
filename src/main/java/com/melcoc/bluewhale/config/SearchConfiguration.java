package com.melcoc.bluewhale.config;

public class SearchConfiguration {

//    <select id="getUser_id" parameterType="int" resultType="" >
//            　　　　SELECT id,nick_ame FROM user WHERE id IN (SELECT MIN(id) FROM user WHERE nick_name like #{nick_name} GROUP BY nick_name)
//    </select>
}
