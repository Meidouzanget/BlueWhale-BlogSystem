spring:
  mvc:
    view:
      prefix: /
      suffix: .html
---
spring:
  datasource:
    url: jdbc:mysql://202.81.231.187/bluewhale?useTimezone=true&serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: bluewhale
    password: 56GrciF87xKH3cZM
    driver-class-name: com.mysql.cj.jdbc.Driver

#mybatis的配置mybatis.mapper-locations=classpath:mapper/*.xml
#Mybatis 的配置
mybatis:
  #配置驼峰命名法
  configLocation: #此处不能用局部configuration变量来配置
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:com/melcoc/bluewhale/mappers/*.xml
  config-location: classpath:src/main/resources/mybatis/mybatis-config.xml

  type-aliases-package: com.melcoc.bluewhale.domain

#show sql
logging:
  level:
    com:
      hp:
        dao:
          DEBUG